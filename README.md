# Mac OS command crawler

* Usage: node index.js
* Option:
  - typo generate number:
    * default value is "10"
    * e.g. node index.js 5
    * Perhaps cause "heap out of memory" when set too large number
